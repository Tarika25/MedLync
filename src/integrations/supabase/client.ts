// Supabase has been replaced with PostgreSQL backend (MedLync).
// All API calls go through the medlync backend server at http://localhost:3001/api
// 
// Import API functions like this:
// import { signup, login, listPrescriptions } from "@/lib/api";
//
// The backend handles all database operations via PostgreSQL directly.

export const apiBase = "http://localhost:3001/api";

